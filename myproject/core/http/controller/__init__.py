"""
Views package for the Core application.

This package organizes all view logic by domain using subfolders:
- auth/:   Authentication-related views (login, register, logout)
- admin/:  Administrative views
- user/:   User-specific views (dashboard, profile, settings)

Views in this package handle HTTP requests and return HTTP responses.
For API endpoints, see core/api/views/.
"""

# Export views for convenient imports
# from core.views.auth.login import LoginView
# from core.views.auth.register import RegisterView
